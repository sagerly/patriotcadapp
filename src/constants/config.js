// src/constants/config.js
export const API_CONFIG = {
  // Update this URL to match your deployed Django backend
  BASE_URL: 'https://test.patriotmdt.com',
  ENDPOINTS: {
    LOGIN: '/login',
    REGISTER: '/register',
    LOGOUT: '/logout'
  }
};